# Playwright Tests

This repository contains automated tests written with Playwright and TypeScript.

## Installation

Install project dependencies:

npm install

Install Playwright browsers:

npx playwright install

## Running Tests

Run Unit 10 tests:

npm run test:unit10

## Running Tests in UI Mode

npm run test:unit10:ui